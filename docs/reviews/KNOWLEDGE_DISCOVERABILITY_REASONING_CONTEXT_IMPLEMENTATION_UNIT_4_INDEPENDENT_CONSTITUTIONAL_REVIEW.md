**Status:** Independent Constitutional Review of Implementation Unit 4. This review is limited to the one-file production-composition verification and grants no authority to begin or accept Unit 5.

# Knowledge Discoverability and Reasoning Context Implementation Unit 4 - Independent Constitutional Review

## 1. Reviewed evidence

```text
base=c324255342fdcea2f7059184e0539a8465a9efa8
unit=e7265f492d83abaee7878e75ab0e60a71b20a5bc
```

The review independently inspected:

- the complete one-file diff and all 17 tests;
- the accepted Unit 3 production graph the tests observe;
- Contract Design Sections 7 and 14;
- corrected Scope Lock Sections 4, 7, 9, and 11;
- corrected Implementation Plan Section 9 and every inherited stop condition;
- the mapper, policy, engine, identity, Purpose, and purpose-bound-view mechanics used by the least-authority proof;
- focused and full-suite evidence from the Parker server and an independent Windows full-suite run.

## 2. Test-only authority boundary

The unit adds one new composition test and changes no production or existing test file. It constructs no production wiring and repairs no Unit 3 behavior. Reflection is limited to established observation of the composed graph for construction and shared-instance proofs.

The least-authority proof does not reflect into `ParkerRuntime` authorization state. Its independent graph uses real production classes and the exact governed values, without an accessor, widened visibility, raw capability exposure, or alternate algorithm.

The proposed Unit 5 integration file is unchanged. No later-unit work is present.

## 3. Authorization and least authority

Positive recall is proven against the real composed source and shared persistence. Negative recall is proven through the real composed engine. The Purpose matrix demonstrates fail-closed behavior across every required absent, wrong, unregistered, and inactive case without asserting an impossible mixed per-item outcome.

For Document denial, the principal is directly confirmed `ACTIVE`, the Purpose is active, and the real vocabulary and mapper resolve the exact READ/DOCUMENT action. Therefore the observed direct-policy `DENIED` cannot be explained by identity rejection, inactive Purpose, unknown action, or resource mismatch.

The purpose-bound wrapper fetches the delegate's genuine Document exactly once with the same principal and ID, then returns `null`. No Document content crosses the boundary, and no reasoning-source Document capability is introduced. The pre-existing guard remains decisive.

## 4. Composition integrity

Shared-instance assertions establish that promoted Knowledge Items are visible to the reasoning source without parallel persistence. The permission-engine identity is likewise shared. Lifecycle, staleness, and repeated-order tests exercise the real composed source rather than a fake.

The assembler has only the new reasoning source, and production does not construct the legacy store. Evidence Intelligence remains unwidened in the same runtime after successful recall. Knowledge Submission and candidate-evaluation behavior remain covered by passing unchanged suites.

## 5. Stop-condition challenge

No inherited or Unit 4 stop condition is triggered:

- exactly one authorized new test file exists;
- no production repair or additional file is present;
- all tests make meaningful, falsifiable assertions;
- no real-policy mixed-item claim is made;
- the Document proof uses an independently constructed real graph;
- its principal and Purpose are active;
- action mapping is directly proven non-vacuous;
- delegate fetch and nondisclosure are both directly proven;
- no private Parker authorization access or visibility widening occurs;
- Unit 5 has not begun;
- focused and full suites pass.

## 6. Findings and verdict

```text
P0=0
P1=0
P2=0
P3=0
VERDICT=ACCEPTED
```

Implementation Unit 4 at `e7265f492d83abaee7878e75ab0e60a71b20a5bc` is constitutionally aligned with the corrected Scope Lock, corrected Implementation Plan, unchanged Contract Design, and accepted Unit 3 composition.

Acceptance is limited to Unit 4's production-composition proof. It does not establish the separately required Unit 5 conversational recall proof, live-model behavior, restart durability, durable auditing, programme completion, or closure. Unit 5 remains prohibited until both Unit 4 reviews are accepted and merged.
