**Status:** Independent Constitutional Review of the Unit 3-D Comparative Evaluation Scope Lock — **ACCEPTED WITH QUALIFICATIONS (non-blocking).** This review independently re-derives every load-bearing claim from primary sources — the actual Attempt 6 durable artifacts, the actual Family C Offline Completion Determination and its own Independent Constitutional Review, the actual Unit 3-A/3-B/3-C governance text — rather than proofreading the Scope Lock's own prose. No comparative evaluation was performed. No remedy was ranked or selected. No code, test, Gradle, or Unit 3-C artifact was modified. No model or HTTP call occurred.

# Unit 3-D Comparative Evaluation Scope Lock — Independent Constitutional Review

## 1. Method

Independently re-read the full Scope Lock text against its own thirteen sources (`ab27f18`, `55af571`, `fee2edd`, `0072c01`, `144cf51`, `b5c0749`, `239c6c1`, `ccacf47`, `911d1c6`), fetching each cited document fresh rather than trusting the Scope Lock's own citations. Independently re-derived the exposure table (Section 4 of the Scope Lock) from Attempt 6's own durable artifacts a further time, using a fresh `wc -l` pass distinct from any script used in either the Planning Review or the Family C Determination. Independently re-checked the matched-fixture subset by re-deriving fixture-level completion counts per arm directly from `raw.jsonl`, not accepted from the Scope Lock's own restated table.

## 2. Adversarial challenge 1 — is the matched-subset restriction justified, or arbitrarily narrow?

Independently re-derived per-fixture completion counts for Control, Family A's own decision sub-step, and Family B directly from `/var/lib/parker/reasoning-protocol-live-model/unit3c-remedy-experiments-20260810-03/{control,family-a,family-b}/raw.jsonl`: exactly five fixtures (`r01-direct`, `r02-please`, `r03-dont-forget`, `p01-ordinary-fact`, `p02-quoted-remember`) show `n=5` in all three arms; every other fixture shows `n=0` in at least one arm (Family A in particular reaches only five distinct fixtures at all before its own checkpoint). **Independently confirmed accurate, not merely restated.** The restriction is not arbitrary — it is the largest fixture set satisfying strict full-repetition matching across the three model-invoking arms, and enlarging it to include partially-matched fixtures (e.g., treating Family A's own single `p03-ambiguous-memory` decision attempt as "close enough" to Control's and Family B's own five) would reintroduce exactly the uneven-denominator comparison Unit 3-B Section 10 already prohibits. **Not falsified.**

## 3. Adversarial challenge 2 — is unequal exposure handled honestly, or minimized?

Independently re-read Section 4's own exposure table against a fresh re-count: Control 132/145 (91.0%), Family A 51/220 (23.2%), Family B 91/115 (79.1%), Family C live 6/29 (20.7%) — all independently reproduced exactly. Independently checked whether the Scope Lock's own Section 4 "Prohibited, absolutely" clause is enforceable in practice, not merely aspirational: it requires denominators and exposure fractions adjacent to every per-arm figure in every future output, which is a concrete, checkable textual requirement a future Independent Constitutional Review of an actual comparative-evaluation report could mechanically verify (grep for a bare percentage or count with no accompanying denominator). **Not minimized; honestly and checkably disclosed.**

## 4. Adversarial challenge 3 — is censoring correctly classified?

Independently re-derived the censoring classification from first principles, before comparing to Section 6: the safety checkpoint's own trigger condition (`isAdversarialCategoryFalsePositive`) is a deterministic function of the exact outcome variable (false-positive REMEMBER/GOAL) a semantic comparison would measure — this is the textbook definition of informative censoring, not the weaker "missing at random" case standard confidence-interval or survival-analysis machinery typically assumes without adjustment. Independently checked the alternative characterization the Scope Lock does *not* adopt — treating the halt as simple truncation without the "informative" qualifier — and confirmed this would understate the bias: a naive truncation-only framing might permit inferring the observed portion is a fair random sample of what the full schedule would show, which the informative-censoring framing correctly forbids (Section 6, rule 3). **Correctly classified, independently re-derived, not merely asserted.**

## 5. Adversarial challenge 4 — did any prohibited aggregate comparison slip through the Scope Lock's own text?

Independently scanned every table and sentence in the Scope Lock for a raw, unqualified arm-wide semantic figure presented as directly comparable to another arm's own differently-composed figure. Found none presented as a comparison — every semantic figure cited (Section 8's 14/26 and 24/24 for Family A; the false-positive occurrence facts in Section 11) is either explicitly intra-arm (Family A's own two sub-steps) or explicitly scoped to the matched subset or to a single arm's own standalone characterization (Family C's 24/29). **No violation found in the Scope Lock's own drafting**, though this is a necessary, not sufficient, guarantee — Section 15's own output-requirements list is what will need to be checked against an *actual future report*, which does not yet exist and is not evaluated by this review.

## 6. Adversarial challenge 5 — is Family A aggregation actually prevented, or merely discouraged?

Independently examined whether Section 8's own prohibition is structural (the report format itself cannot produce a blended figure) or merely advisory (a future author could still choose to compute one). It is advisory, as written — a text-level prohibition, not a code-level or schema-level constraint (no data structure exists that would refuse to accept a blended Family-A score). This is judged appropriate for a governance-tier document (Unit 3-D produces reports, not code, so no structural enforcement mechanism is available at this layer), but it means the eventual comparative-evaluation task's own Independent Constitutional Review must independently re-check this specific prohibition against the actual report text — a burden this Scope Lock correctly places on that future review (Section 16, item 10) rather than pretending to close permanently here. **Not falsified as a governance instrument; flagged as inherently reliant on future enforcement, which the Scope Lock itself already anticipates.**

## 7. Adversarial challenge 6 — is Family C's own asymmetry handled fairly, in both directions?

Independently checked for bias in both directions, as the governing task explicitly requires: **toward Family C** — Section 9 explicitly forbids treating zero-model-calls as superior reliability and requires architectural qualification on every latency/timeout comparison; independently confirmed this qualification appears in Section 7's own latency/cost row and is not merely asserted once and dropped. **Against Family C** — checked whether the Scope Lock inappropriately penalizes Family C for its own architectural difference (e.g., by excluding it from comparisons it could legitimately participate in): Section 9 item 3 explicitly preserves Family C's participation in the matched-subset comparison at the five shared fixtures, and item 6 credits the now-closed nine-category adversarial coverage without qualification. Independently re-confirmed via the Family C Offline Completion Determination's own Independent Constitutional Review (`ccacf47`, Section 7 there) that the nine-category closure claim is itself independently verified via four distinct methods, not merely asserted once. **Balanced in both directions; not falsified.**

## 8. Adversarial challenge 7 — is offline/live provenance actually preserved, or does the Scope Lock's own language create a loophole?

Independently re-read Section 3's evidence-source table and Section 9's own provenance rule together, specifically hunting for a phrasing gap that would let a future report present "Family C: 24/29 correct" without a source label. Found none — Section 3 states source B "Never [is] attributable to, or blended with, campaign `unit3c-remedy-experiments-20260810-03` itself," and Section 15 item 7 requires the future report's own Family-C section to state "live and offline results individually labeled." Independently stress-tested the edge case of the five matched-subset fixtures, all of which are live (Source A) for Family C — confirmed Section 5's own text correctly notes this ("all five already available from live evidence... independent of the offline completion"), avoiding a subtle version of exactly the blending risk this challenge probes. **Not falsified.**

## 9. Adversarial challenge 8 — does timeout handling avoid survivorship bias?

Independently re-derived Family A's own decision-step figures a further time from raw artifacts to check whether the one genuine timeout (`family_a/r02-please/render/02`) could silently distort a completion-conditioned figure: the timeout occurred in the *render* sub-step, not the *decision* sub-step, so Family A's own decision-step semantic figures (14/26) are entirely unaffected by it — independently confirmed by re-reading the timeout record's own `trialId`. Section 10 item 4's own survivorship-bias requirement (pairing completion rate with any completion-conditioned semantic figure) is independently judged sufficient and correctly targeted, since it is written generally (applying to any future arm/metric combination that might exhibit this pattern), not narrowly patched to this one already-known case. **Not falsified.**

## 10. Adversarial challenge 9 — is false-positive timing being misused as ranking, anywhere?

Independently re-read Section 11 sentence by sentence for any residual comparative framing despite its own explicit prohibitions. Found one place worth flagging as a **non-blocking qualification** (Section 14 of this review): Section 11's own closing permitted-statements paragraph states Family C's live trigger is "independently, definitively confirmed... unlike Control's, Family A's, and Family B's own triggers, whose specific `actualAction` values are *also* now definitively durably recorded" — this is correctly framed as a *durability-mechanism* fact (all four arms' triggers are now equally well-recorded, post-correction), not a safety-ranking fact, and the sentence explicitly says so ("a durability-correction fact, not a safety-comparison fact... must not be conflated with one"). Independently judged this self-qualification adequate on the page, but worth flagging because the sentence's own structure (naming Family C first, "unlike" the other three) has a surface shape a careless future reader could still misread as ranking-adjacent despite its own explicit disclaimer. **Not a blocking defect** — the disclaiming language is present and correctly placed in the same sentence, not a separate footnote — but the phrasing itself is denser than ideal for a rule meant to prevent exactly this kind of misreading.

## 11. Adversarial challenge 10 — is statistical authority overstated anywhere?

Independently checked Section 12's own prohibited-claims list against Unit 3-A Section 4 and Unit 3-B Section 6, re-read fresh: confirmed the qualification-tier thresholds cited (≥300-exposure zero-event gates, ≥99% representation validity, ≥97% one-sided-95%-LCB) are quoted correctly and the prohibition on applying them as a Unit 3-D bar is independently judged correct — these thresholds govern production qualification, a later, separate, higher tier this exploratory evidence was never designed to meet, and Unit 3-B Section 6 explicitly states exploratory evidence "may never, by itself... establish production reliability." Independently checked whether Section 12's own "permitted" list smuggles back any inferential overreach: it permits only "exact observed counts and rates, always with explicit denominators" and "descriptive statements of what a given figure does and does not, by itself, establish" — independently judged this is genuinely descriptive-tier language, not disguised inference. **Not overstated; not falsified.**

## 12. Adversarial challenge 11 — is `contentFidelity` improperly inferred anywhere?

Independently re-confirmed via direct inspection of five real `raw.jsonl` records (one per arm, sampled fresh for this review) that `"contentFidelity":null` appears literally in every one, across both live and offline-adjacent (Family C) sources. Independently re-read Section 13's own prohibition list ("invent, estimate, proxy, or infer... not from representation validity, not from semantic correctness, not from qualitative reading of any raw text") and confirmed this closes the most plausible proxy routes a future task might otherwise attempt (e.g., "representation was valid, so content was probably faithful" — explicitly forbidden). **Not falsified.**

## 13. Adversarial challenge 12 — did remedy-selection language leak in anywhere?

Independently scanned the full Scope Lock text for the specific terms this programme has already flagged as selection-adjacent ("best," "preferred," "recommended," "wins," "superior," "outperforms") and for softer variants ("stronger," "weaker," "more reliable," "safer"). Found zero instances used in a cross-arm comparative sense; the word "weaker" appears once, in Section 12, describing Family A's decision-step performance *relative to its own render step* (an explicitly-permitted intra-arm comparison per Section 14's own carve-out), not relative to any other family. **No leakage found.**

## 14. Adversarial challenge 13 — is Unit 3-E authority genuinely preserved, or narrowed/expanded by implication?

Independently re-read the programme-level Unit 3 Planning Review's own Section 11 boundary statement a second time, character by character, and compared it against Scope Lock Section 14: the Scope Lock's own firewall language ("Unit 3-D may... Unit 3-D must NOT... These functions belong exclusively to Unit 3-E") is independently confirmed to restate, not narrow or expand, the programme-level boundary — it does not, for instance, grant Unit 3-D any implicit veto or pre-selection influence over Unit 3-E's own eventual choice (e.g., by mandating a specific report structure that would functionally pre-rank remedies through table ordering or emphasis). Independently checked Section 15's own output-requirements ordering (evidence sources, exposure, censoring, matched-subset, arm-wide, Family A, Family C, timeout, false-positive, representation, limitations, unresolved questions, no-selection statement) for any structure that would present one family first/most-favorably by construction: the ordering is Control-Family-A-Family-B-Family-C throughout, matching the already-frozen arm order (`UNIT_3C_ARM_ORDER`), not a performance-based ordering. **Unit 3-E's authority is preserved intact; not falsified.**

## 15. Discrepancies found

None requiring correction to the Scope Lock's own substantive content. One phrasing density concern (Section 10 of this review, challenge 9) is recorded as a non-blocking qualification.

## 16. Blocking defects

None.

## 17. Non-blocking qualifications

1. Section 11's own closing paragraph (durability-recording fact vs. safety-comparison fact) is substantively correct and self-disclaiming but densely phrased; a future comparative-evaluation report drawing on this section should restate the distinction in simpler, more separated sentences rather than reusing the Scope Lock's own compressed phrasing verbatim, to reduce the risk of a reader extracting only the "unlike Control..." clause without its own qualifier.
2. Section 8's Family A aggregation prohibition (Adversarial Challenge 5) is a text-level, not structural, constraint — this is an inherent, correctly-acknowledged limitation of governance-tier documents in this program generally, not specific to this Scope Lock, but the future comparative-evaluation task's own Independent Constitutional Review must treat re-verifying this specific prohibition as a first-class checklist item, not an assumed-satisfied inheritance from this Scope Lock's own existence.
3. Section 3's evidence-source table classifies Source E ("semantic evidence") as usable for "intra-arm... comparison unrestricted by the matched subset" — this is correct as written, but a future task should take care that "intra-arm" is read narrowly (Family A's own two sub-steps; Family C's own full-corpus internal characterization) and not stretched to cover, for instance, a comparison of Control's own performance on the matched subset versus Control's own performance on its full 132-trial attempted set presented in a way that invites an implicit cross-arm inference through the back door. Not a defect in the Scope Lock's own text, which does not do this, but a boundary worth naming explicitly for the next reviewer.

## 18. Verdict

```text
ACCEPTED WITH QUALIFICATIONS
```

The qualifications in Section 17 are non-blocking — they do not indicate any constitutional defect in the Scope Lock's own frozen boundary, methodology, or firewall, and do not require this document's own text to change before it may be relied upon. They identify specific points a future comparative-evaluation task's own Independent Constitutional Review must verify with particular care, given that this Scope Lock's own enforcement mechanisms are necessarily textual rather than structural at this governance tier.

## 19. Confirmation

No comparative evaluation was performed during this review. No remedy was ranked, preferred, or selected. No model or HTTP call occurred. No campaign was created, resumed, or modified — Attempt 6's own artifacts were inspected read-only, independently re-confirmed unchanged (26 files). No production, test, or Gradle file was modified. No Unit 3-C artifact, fixture, or mechanism was altered. Unit 3-E and Unit 4 were not begun. The Scope Lock document itself was not modified by this review.
