**Status:** Genuine Independent Constitutional Review of the Unit 2 Scope Lock. The frozen definitions were challenged against the accepted Planning Review and committed Unit 1 observation boundary; the Scope Lock was evidence, not authority.

# Reasoning Protocol Live-Model Conformance Unit 2 Scope Lock — Independent Constitutional Review

## 1. Independent boundary

Unit 2 may measure one pinned current configuration, preserve every adverse result, and stop at observations. It may not tune, repair, substitute, execute downstream, or choose Unit 3's remedy.

## 2. Challenges

| Challenge | Finding |
|---|---|
| Definitions frozen before results | **PASS.** All 23 IDs/texts/actions/rubrics, nine exact profiles, stages, counts, and trial identities are fixed before execution. |
| Sentinels genuinely pre-registered | **PASS.** Twelve IDs are enumerated and cannot be selected from Stage 1 outcomes. |
| Accidental tuning | **PASS.** Request shape and single model/digest are immutable; sampling, schema, prompt, parser, retries, and substitution are forbidden. |
| Batching/resume bias | **PASS.** Schedule is precomputed; only missing deterministic IDs execute; completed H/I/wrong/malformed results are never retried. Identity mismatch rejects continuation. |
| False-positive evidence | **PASS.** Raw evidence is flushed and hashed before pause; genuine events remain in place and resume starts at the next ID. |
| Timeout separation | **PASS.** 90 seconds is evaluation-only; H is operational and Parker's default is unchanged. |
| Artifact trust | **PASS.** Per-call flush, duplicate rejection, manifests, checksums, chained batch hashes, and durable synthetic-only storage are mandatory. |
| Fidelity independence | **PASS.** Deterministic checks plus two blinded human reviewers and third resolution avoid model self-grading and aggregate-result influence. |
| Model identity pinned | **PASS.** Digest/equivalent manifest is mandatory before scoring and must match every batch. Tag name alone is rejected. |
| Unit 1 untouched | **PASS.** Only its public APIs are composed; both Unit 1 files are forbidden. A harness problem returns through Unit 1 defect governance. |
| Unit 3 leakage | **PASS.** Remedy families and decisions are excluded even if performance is catastrophic. |
| Size/feasibility | **PASS.** 3,900 scored calls provide 30 per cell; staged design is smaller than full cross-product; fourteen ≤300-call batches support a roughly 34–44-hour observed runtime. |

## 3. Adversarial observations

The production-selection-guidance and minimal profiles intentionally share empty context because selection guidance is invariant in the real builder. Treating them as two different hand-written prompts would violate scope; the lock correctly treats one as a control.

The false-positive pause is not outcome-based early termination: after independent confirmation, the same frozen campaign resumes unchanged. Conversely, retrying the offending call would bias the evidence and is prohibited.

The one-file driver is not yet accepted implementation. The requirement for per-call durable append and manifest recovery is feasible through public Unit 1 serialization/execution APIs, but the later Boundary Review must prove it without editing Unit 1.

## 4. Verdict

```text
ACCEPTED
```

Corrective action: **NONE**. No implementation or execution is authorized by this review.
