**Status:** Independent Constitutional Review of the Family F Model Role and Research Question Scope Lock — **REVISE BEFORE ACCEPTANCE.** Every material proposition governing the frozen model roles, the corrected research question, the historical-evidence boundary, the lighthouse boundary, and the hold on the pending Implementation Authorization Decision was independently re-derived from primary sources and found sound. One precise, bounded defect was found in Section G's downstream-correction-consequences claim: it names "exactly two" upstream instruments as requiring bounded correction and states the pending Decision's own frozen input table is "drawn directly from them," but the pending Decision's own committed text states its controlling authority is the Bounding Evidence Acquisition and Offline Estimator Plan and the Capture-Proxy Response/Request-Size and Dedicated-Runtime-Growth Bounding Scope Lock — neither of which Section G names — and both of those documents independently freeze their own `SUBJECT_MODEL=qwen2.5-coder:7b`/`CONTROL_MODEL=llama3.2:3b` tables as binding governance facts, not merely inherited references. This is the "downstream dependency ambiguity" risk the governing task itself named as a specific thing to check for, and it is confirmed present. The Scope Lock's own frozen content (Sections A–F, H, I) is not implicated and does not require revision. No document was edited by this review. The target Scope Lock was not modified. The pending Implementation Authorization Decision was not modified. No model was called. Nothing was staged, committed, or pushed.

# Family F Model Role and Research Question Scope Lock — Independent Constitutional Review

## 1. Independent evidence reviewed

Read fresh for this review, not accepted from the target Scope Lock's own restatement:

```text
$ git log -1 --format="%H %s"
083b94dbd25c4fe74ed4e6310b8646c07a8cb00a docs(governance): propagate model identity premise correction

$ git status --short --branch
## main...origin/main
?? docs/architecture/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_FAMILY_F_MODEL_ROLE_AND_RESEARCH_QUESTION_SCOPE_LOCK.md
?? docs/decisions/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_BOUNDING_EVIDENCE_IMPLEMENTATION_AUTHORIZATION_DECISION.md
```

- `docs/reviews/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_MODEL_IDENTITY_PREMISE_DEFECT_CONFIRMATION_REVIEW.md` and its accepted ICR — re-read for the underlying defect and its own affected-document reasoning.
- `docs/reviews/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_FAMILY_F_ALTERNATIVE_MODEL_DIAGNOSTIC_PLANNING_REVIEW.md` — re-read Sections 12, 18, and its own model-identity-premise-correction addendum.
- `docs/architecture/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_EXPERIMENTAL_RECLASSIFICATION_AND_QUALIFICATION_BOUNDARY_SCOPE_LOCK.md` — re-read Sections 7–8 in full, fresh.
- `docs/architecture/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_CAPTURE_PROXY_RESPONSE_REQUEST_SIZE_AND_DEDICATED_RUNTIME_GROWTH_BOUNDING_SCOPE_LOCK.md` — re-read Section 4 ("Frozen campaign invariants") in full, fresh.
- `docs/implementation/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_BOUNDING_EVIDENCE_ACQUISITION_AND_OFFLINE_ESTIMATOR_PLAN.md` — re-read Section 4 ("Frozen inputs and statuses") in full, fresh.
- `docs/decisions/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_BOUNDING_EVIDENCE_IMPLEMENTATION_AUTHORIZATION_DECISION.md` — re-read its own Section 1 controlling-authority statement in full, fresh.
- The target Scope Lock itself, in full.

```text
$ grep -n "AUTHORIZ" docs/architecture/..._FAMILY_F_MODEL_ROLE_AND_RESEARCH_QUESTION_SCOPE_LOCK.md
(one hit: the pending Decision's own filename, referenced by name only — no
affirmative grant language anywhere in the target document)
```

## 2. Findings against each role assignment (Questions 1–4)

**qwen2.5-coder:7b as `DEPLOYED_BASELINE`:** independently re-confirmed against the accepted Model-Identity Premise Defect Confirmation Review's own committed evidence (`docker-compose.yml`'s `PARKER_MODEL_NAME` default, fixed since `6fd57dc`, 2026-08-03, six days before RPLMC's own first commit) — correct.

**qwen2.5-coder:7b as `CONTROL_MODEL`:** this is a policy definition, not an independently-provable fact, and it is internally coherent — a comparison intended to answer "would switching away from what's actually deployed help" is only meaningful measured against what's actually deployed. No contradiction found anywhere in cited governance.

**llama3.2:3b as `SUBJECT_MODEL`, without constituting fresh selection:** independently confirmed not a fresh nomination. The already-accepted Family F Alternative-Model Diagnostic Planning Review (Section 12, re-read fresh) already named `llama3.2:3b` as a "proposed comparison control... because it produced the accepted Attempt 2 operational evidence and is the current comparison point in DQ4" — i.e., already-accepted governance already put this specific model into Family F's evidentiary frame; the target Scope Lock only corrects which *role* it occupies, not whether it is present at all. Section B's explicit disclaimers (not preferred, not selected, not approved, not production-track) are consistent with, and do not overstate, this.

**Distinction between deployed baseline / diagnostic control / diagnostic subject / selected remedy:** independently confirmed preserved — Sections B and D of the target Scope Lock keep these four concepts textually and functionally separate throughout; no sentence collapses any two of them.

## 3. Findings on the corrected research question (Questions 6–8)

Independently re-checked against Unit 3-B's own original Family F classification (model/provider substitution, one candidate remedy family among several) and Unit 3-A §11's qualification-tier requirement. The frozen question (Section E) stays within that existing classification — it does not create a new programme, does not assume `llama3.2:3b` will outperform `qwen2.5-coder:7b` (explicitly disclaimed, and consistent with `DQ4`'s own actual finding that both models missed the same fixture once each), does not convert exploratory evidence into qualification evidence (Section D.4 explicit), and does not convert comparison into selection (Section D.1–D.2, D.6 explicit). Family F remains diagnostic/screening only, consistent with every prior accepted Family F document's own self-description.

## 4. Findings on historical-evidence preservation (Question 5)

Independently re-checked: the target Scope Lock does not restate, recompute, or reinterpret any specific historical figure (fixture counts, `DQ4`'s outcome, Attempt 2's captured tag, RAM readings) anywhere in its own text — Section C states the boundary at the correct level of generality and does not itself touch the underlying numbers. No historical document is edited by this Scope Lock (confirmed: `git status` shows only the target Scope Lock and the pre-existing untouched pending Decision as untracked; nothing else in the tree is modified).

## 5. Findings on preserved Family F machinery (Question 9)

Independently spot-checked two of the eight claims in Section H against primary text: the capture-proxy byte-budget arithmetic (`46 cells × 4 repetitions × 2 roles = 368 scored calls`, `8 residency blocks × 3 warm-up calls = 24 warm-up calls`) is confirmed symmetric over the two roles in both the Capture-Proxy Bounding Scope Lock's own Section 4 and the Bounding Evidence Acquisition Plan's own Section 4 — it does not depend on which physical model is named `CONTROL_MODEL` vs `SUBJECT_MODEL`, only on there being two. The Experimental Reclassification Scope Lock's own absolute advancement gate (`≥3-of-4 per cell`) is likewise unconditioned on which model plays which role. Section H's claims are independently confirmed accurate.

## 6. Downstream correction completeness assessment (Question 10) — DEFECT FOUND

This is the one adversarial check this review treats as decisive, mirroring the governing task's own explicit instruction to check for exactly this.

Independently re-read, fresh, the pending Decision's own Section 1: *"This decision must be read with the controlling Plan, the accepted Capture-Proxy Response/Request-Size and Dedicated-Runtime-Growth Bounding Scope Lock and its ICR, and `docs/architecture/parker-constitution.md`."* The pending Decision names its own controlling authority explicitly, and it is **not** the Family F Alternative-Model Diagnostic Planning Review and **not** the Experimental Reclassification and Qualification-Boundary Scope Lock — the two, and only two, instruments the target Scope Lock's Section G names.

Independently re-read, fresh, both of the documents the pending Decision *does* name:

```text
Capture-Proxy Bounding Scope Lock, Section 4 ("Frozen campaign invariants"):
  SUBJECT_MODEL=qwen2.5-coder:7b
  CONTROL_MODEL=llama3.2:3b
  (plus FIXTURE_COUNT, SCORED_CALLS, TOTAL_CALLS, ADVANCEMENT_GATE, ...)

Bounding Evidence Acquisition and Offline Estimator Plan, Section 4
  ("Frozen inputs and statuses"):
  SUBJECT_MODEL=qwen2.5-coder:7b
  CONTROL_MODEL=llama3.2:3b
  (plus the identical FIXTURE_COUNT/SCORED_CALLS/TOTAL_CALLS figures,
   FROZEN_SCHEDULE, PRODUCTION_FORMATTER, PRODUCTION_REQUEST_SERIALIZER)
```

Both documents independently and explicitly freeze the same backward `SUBJECT_MODEL`/`CONTROL_MODEL` pair as their own binding governance fact — not as an incidental restatement, but under headings ("Frozen campaign invariants," "Frozen inputs and statuses") that are exactly as load-bearing in kind as the Experimental Reclassification Scope Lock's own Section 8. The actual dependency chain, independently reconstructed from each document's own citations, is:

```text
Experimental Reclassification and Qualification-Boundary Scope Lock (Section 8, originates the naming)
  -> Capture-Proxy Bounding Scope Lock (Section 4, independently re-derived and re-frozen against it)
  -> Bounding Evidence Acquisition and Offline Estimator Plan (Section 4, independently checked
     against the Capture-Proxy Bounding Scope Lock's own Section 4 — its own ICR states this exactly)
  -> pending Implementation Authorization Decision (Section 1, names the Plan and the Capture-Proxy
     Bounding Scope Lock as its own controlling authority)
```

The Family F Alternative-Model Diagnostic Planning Review is a genuine, earlier ancestor of the *concept* (it first proposed the two model identities as planning labels) but is not in the pending Decision's own stated *numeric-table* lineage at all.

**Consequence:** Section G's claim — "the Decision's own frozen input table is drawn directly from them [the Planning Review and the Reclassification Scope Lock]" — is factually inaccurate, and its list of documents "requiring bounded correction before the pending Decision can be corrected" is incomplete: it omits the Capture-Proxy Bounding Scope Lock and the Bounding Evidence Acquisition and Offline Estimator Plan, both of which sit directly between the Reclassification Scope Lock and the pending Decision in its own stated chain, and both of which independently freeze the same backward values that would need the identical corrective swap.

This is precisely the "downstream dependency ambiguity" (risk I) the governing task named as a specific thing to check for, and it is confirmed present, not merely suspected.

**Why this rises to REVISE, not a non-blocking qualification:** unlike the earlier, accepted-with-qualification finding on the Model-Identity Premise Defect Confirmation Review (where the affected-document list was advisory and explicitly non-exhaustive by the document's own admission), Section G here makes a specific, load-bearing causal claim ("drawn directly from them") that gates exactly when the pending Decision may be corrected. A future task relying on Section G as written would correct only two documents, conclude the pending Decision's own upstream chain is now consistent, and be wrong — two more documents in that Decision's own stated controlling chain would still carry the old, unswapped values. That is a genuine risk of premature or incomplete downstream action, not a cosmetic gap.

## 7. Lighthouse-provenance assessment (Questions 12–13)

Independently confirmed properly bounded. Section F states only a bare fact set (input, deployed model, observed outcome, persistence result) with the capability-in-principle statement, and explicitly disclaims corpus membership, Attempts-1–2 membership, retroactive addition to Unit 2, evidence promotion, and execution authority — all four disclaimers independently checked against the document's own text and found present and unqualified.

## 8. Constitutional-risk assessment

| Risk | Finding |
|---|---|
| A. Role-correction-as-disguised-selection | Not present — Section B's disclaimers are explicit and consistent with the rest of the document. |
| B. Historical evidence rewriting | Not present — confirmed no historical document edited, no historical figure restated. |
| C. Candidate preference leakage | Not present — Section D.3 explicitly declines to predict outcome. |
| D. Exploratory evidence inflation | Not present — Section D.4 explicit. |
| E. Lighthouse provenance leakage | Not present — see Section 7 above. |
| F. Execution authority leakage | Not present — Section A explicit; independently confirmed via `grep` that no affirmative `AUTHORIZ*` grant exists anywhere in the document. |
| G. Research-question scope creep | Not present — see Section 3 above. |
| H. Terminology drift between roles and actual deployed status | Not present — Section B's definitions are precise and internally consistent. |
| **I. Downstream dependency ambiguity** | **Present — see Section 6 above. This is the defect requiring correction.** |

## 9. Verdict

```text
REVISE BEFORE ACCEPTANCE
```

## 10. Defect and minimum correction (not performed by this review)

**Defect:** Section G's list of "the two identified downstream instruments" (Family F Alternative-Model Diagnostic Planning Review; Experimental Reclassification and Qualification-Boundary Scope Lock) is incomplete relative to the pending Implementation Authorization Decision's own stated controlling-authority chain, and Section G's claim that the Decision's frozen input table is "drawn directly from" those two documents is factually inaccurate.

**Minimum correction proposed (for the document's author to perform in a future task — not performed here):**

1. Add the Capture-Proxy Response/Request-Size and Dedicated-Runtime-Growth Bounding Scope Lock (+ its ICR) and the Bounding Evidence Acquisition and Offline Estimator Plan (+ its ICR) to Section G's list of instruments requiring bounded superseding amendment, each citing their own Section 4 frozen table as the specific location.
2. Correct Section G's causal sentence to state the actual chain: Experimental Reclassification and Qualification-Boundary Scope Lock (originates) → Capture-Proxy Bounding Scope Lock (re-freezes) → Bounding Evidence Acquisition and Offline Estimator Plan (re-freezes) → pending Implementation Authorization Decision (names the Plan and the Capture-Proxy Bounding Scope Lock as its own controlling authority) — rather than naming the Planning Review as a direct numeric-table ancestor of the Decision.
3. No other section requires any change. Sections A–F, H, and I are independently confirmed sound and do not need revision.

This correction is narrow, textual, and does not touch the frozen role definitions, the research question, the historical-evidence boundary, the lighthouse boundary, or any preserved-machinery claim.

## 11. Not yet established by this review

Because the verdict is `REVISE BEFORE ACCEPTANCE`, none of the following becomes a frozen governance fact yet:

```text
DEPLOYED_BASELINE = qwen2.5-coder:7b   -- NOT YET FROZEN (pending Scope Lock acceptance)
CONTROL_MODEL     = qwen2.5-coder:7b   -- NOT YET FROZEN
SUBJECT_MODEL     = llama3.2:3b        -- NOT YET FROZEN
CORRECTED_RESEARCH_QUESTION            -- NOT YET AUTHORITATIVE
```

The target Scope Lock's reasoning is independently found sound in every section except Section G; a corrected revision, addressing only the Section G defect identified above, should be able to proceed to a fresh Independent Constitutional Review without re-litigating Sections A–F, H, or I.

## 12. Exact next lawful action

```text
NEXT_LAWFUL_ACTION = Correct Section G of the target Scope Lock per Section 10
                      above, then perform a fresh Independent Constitutional
                      Review of the corrected document.
```

Not performed by this review. This review does not edit the target Scope Lock.

## 13. Verification performed for this review

```text
$ git status --short --branch
## main...origin/main
?? docs/architecture/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_FAMILY_F_MODEL_ROLE_AND_RESEARCH_QUESTION_SCOPE_LOCK.md
?? docs/decisions/REASONING_PROTOCOL_LIVE_MODEL_CONFORMANCE_UNIT_3BF_FAMILY_F_BOUNDING_EVIDENCE_IMPLEMENTATION_AUTHORIZATION_DECISION.md
(this review's own file is new and untracked; nothing else changed)

$ git diff --stat
(no output — no tracked file modified)
```

No model process, campaign, experiment, or implementation action occurred before or during this review. No Gradle build was run. No Ollama server was contacted. No Parker server runtime was touched. The target Scope Lock was not edited. The pending Implementation Authorization Decision was not edited. No downstream Family F document was edited.

## 14. STOP conditions confirmed

```text
NO production code, test, Docker, persistence, QMD, UI, parser, or model
  configuration file touched.
NO model called.
NO Scope Lock edit performed.
NO downstream Family F document edited.
NO edit made to the pending Implementation Authorization Decision.
NO document staged, committed, or pushed.
```
