**Status:** Independent Constitutional Readiness Review — **ACCEPTED**. Conducted directly against the accepted Unit 2 governance, PASS Boundary Review, committed Unit 1 observation boundary, actual implementation diff, and verification evidence. The Implementation Readiness Review was treated as evidence, not authority. No Stage 0 or scored execution occurred.

# Reasoning Protocol Live-Model Conformance Unit 2 — Independent Constitutional Readiness Review

## 1. Independent reconstruction

Unit 2 is constitutionally limited to an evaluation-only instrument that observes the unchanged production reasoning path. It must neither create runtime authority nor improve model behavior. Independently traced from `build.gradle.kts` and `ReasoningProtocolBaselineCharacterisationTest.kt`, the implementation adds one detached filtered task and one test-source-set campaign class. It imports the Unit 1 observation contracts and JDK facilities only. No production source, downstream coordinator, admission path, Goal path, prompt, parser, transport, request options, timeout default, model configuration, dependency, or Unit 1 file changed.

## 2. Authority and consequential reachability — ACCEPTED

The driver can produce only evaluation artifacts. Its live executor terminates in `TrialObservation`. Reflection and source inspection identify no `ParkerRuntime`, `ConversationReplyCoordinator`, `MemoryAdmissionCoordinator`, `MemoryCore`, `KnowledgeSubmission`, planning, execution, tool-dispatch, or repair dependency. Test-only REMEMBER and GOAL observations create no production authority and reach no consequence destination.

The dedicated task/property is activation authority only for this detached evaluation source set. It does not register a runtime action, Purpose, Principal, permission, endpoint, or alternate parser. No test-only mechanism is on a production classpath or lifecycle graph.

## 3. No live campaign execution — ACCEPTED

The Gradle task supplies only the first guard. The live method then requires complete explicit campaign configuration, exact batch, immutable identity evidence, accepted root, timeout, runtime identity, and approval flags before constructing the Unit 1 harness or contacting Ollama. During verification the second guard was absent, so JUnit skipped before client construction. Dry-run evidence proves ordinary lifecycle tasks cannot select the Unit 2 task. Zero live calls occurred; Stage 0 and all 3,900 scored trials remain unexecuted.

## 4. Frozen experiment and absence of result-dependent authority — ACCEPTED

All 23 fixtures, twelve sentinels, nine profile identities, Stage 0 definitions, 3,900 scored cells/attempts, deterministic IDs, fourteen batches, timeout, and production request identity are encoded before an executor is supplied. Canonical serialization and SHA-256 cover the complete ordered definition. Nothing in execution reads an outcome to add, remove, reorder, reclassify, tune, retry, or substitute a future trial. The only outcome-sensitive branch is the constitutionally required stop/pause after durable evidence.

## 5. Exact-once and interruption integrity — ACCEPTED

Independent path trace confirms intent is forced before the single serial endpoint call and raw evidence is forced before checkpoint. A valid raw record is authoritative. Raw-without-checkpoint recovery repairs only local state; it does not infer or repeat inference. Checkpoint-without-raw, duplicate/unknown IDs, identity drift, manifest/hash drift, non-prefix completion, and intent-only interruption all fail closed.

This is the correct constitutional treatment of the unavoidable transaction boundary: an intent-only trial is ambiguous because the endpoint may have completed, so the driver blocks for independent resolution rather than claiming endpoint/local atomicity or silently duplicating the sample. Completed adverse observations are evidence, not retry triggers.

## 6. Manifest, identity, and operator control — ACCEPTED

The campaign lock prevents concurrent runners. The identity fingerprint is byte-exact across repository, model/digest/show evidence, endpoint, timeout, request format, Ubuntu/runtime/container, corpus/profile/campaign hashes, and manifest version. Batch continuation independently verifies every earlier raw artifact and manifest and requires the exact prior-manifest chain. A model tag alone is insufficient; the exact `/api/tags` entry plus hashed `/api/show` evidence is required.

Explicit batch selection and prerequisite seals prevent overlap and “run remaining” ambiguity. Calls are serial; there is no worker, prefetch, retry, background runner, or automatic resume. The fourteen-batch operational design therefore remains feasible without creating autonomous authority.

## 7. Preflight and consequential pause — ACCEPTED

Stage 0 is structurally separate and unscored. A failed warm-up/preflight record is forced and seals a failure state rather than allowing Stage 1. Scoring requires an identity-bound Stage 0 seal and two explicit approvals. An unexpected parsed REMEMBER or GOAL forces raw and consequential evidence, checkpoints and seals the pause, and returns before a next call. The test proves that only one executor invocation occurs in that case.

This preserves constitutional fail-closed behavior without outcome-based sampling bias: the observed trial is not removed or rerun, and the campaign cannot continue automatically.

## 8. Reporting and interpretation boundary — ACCEPTED

Reports regenerate deterministically from immutable raw records. Integer counts remain primary; Wilson intervals use the frozen constant/formula. Confusion, operational, latency/token, repeatability and context-drift outputs remain descriptive. Stable incorrect is distinct from instability. Context categories apply frozen thresholds and avoid causal claims.

The driver does not make semantic judgments about ambiguous paraphrases. It auto-closes only deterministic exact/NOACTION cases, marks absent typed content not assessable, and creates a blinded append-only adjudication worksheet for subjective cases. No model grades another model, and adjudication cannot rewrite raw evidence.

## 9. Adversarial findings and classifications

| Finding | Classification | Acceptance significance |
|---|---|---|
| Ordinary suite fails only at `OcrStructuralIsolationTest.kt:338` on Windows separators | **UNRELATED ENVIRONMENTAL/PORTABILITY ISSUE** | None; pre-existing, outside detached source set, no repository change |
| Campaign artifacts may plausibly consume 40–250 MiB; 500 MiB is a conservative bound | **OBSERVATION ONLY** | The 2 GiB preflight floor is constitutionally conservative |
| Endpoint execution and local persistence cannot be one distributed transaction | **OBSERVATION ONLY** | Correctly addressed by forced intent and fail-closed ambiguous state |
| Human fidelity adjudication is not deterministic | **OBSERVATION ONLY** | Correctly separated from deterministic worksheet generation and immutable raw evidence |

No Unit 2 constitutional defect, Unit 2 non-constitutional defect, hidden production dependency, alternate authority path, result-dependent schedule, silent retry, duplicate-sampling path, or future-remedy commitment was found.

## 10. Verification independence

The constitutional findings were re-derived from the actual code and test behavior:

- Unit 2: 13 tests, 0 failures, one correctly skipped live entry;
- Unit 1: 16 tests, 0 failures, one correctly skipped live smoke;
- ordinary suite: 2,015 tests, only the known OCR portability failure, 8 skipped;
- lifecycle dry-run: detached task absent from `test/check/build/assemble`;
- `git diff --check`: pass; and
- repository diff: exactly the authorized implementation surface plus accepted governance and these two readiness reviews.

## 11. Verdict

```text
ACCEPTED
```

Corrective action: **NONE**.

The implementation is constitutionally safe to become the committed Unit 2 campaign instrument. This acceptance grants no authority to execute Stage 0 or any scored trial. The required next boundary is repository integrity review, commit/push of the accepted instrument with a clean `HEAD == origin/main`, followed only then by explicit live-execution approval.
