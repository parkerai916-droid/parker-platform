**Status:** Genuine Independent Constitutional Review of the Knowledge Candidate Accountable Principal carrier prerequisite. The implementation, contracts, preserved Unit 4 diff and tests were independently challenged against accepted authority; the Completion Review was evidence, not authority. Nothing is staged, committed, or pushed.

# Knowledge Candidate Accountable Principal Prerequisite — Independent Constitutional Review

## 1. Independent scope reconstruction

The prerequisite may close only the evaluator invocation's Principal loss. It may not change candidate payload, submission's public boundary, Purpose, policy, composition, identity registration, retrieval contracts, promotion behavior or Evidence Intelligence. Unit 4 policy work remains paused and cannot be accepted by this review.

Direct diff inspection finds the exact three authorized production files. The only other production diff is the pre-existing preserved Unit 4 `ParkerRuntime.kt` change, whose fingerprint and rule hunks are unchanged from before this prerequisite.

## 2. Challenge: real accountable actor or subsystem identity?

`DefaultKnowledgeSubmission` receives the real explicit Principal and uses it for the submission permission gate. It now forwards that exact value into evaluation. The evaluator performs no autonomous initiation and creates no identity. Every retrieval uses the supplied value.

**Finding:** Principal remains the real accountable submitter, not the executing component.

## 3. Challenge: does Purpose remain independent?

The evaluator contract contains Principal and candidate only. It has no Purpose parameter or field. Production composition still supplies `knowledge-memory.candidate-evaluation` through the immutable Unit 3 view. Changing Principal changes only `ExecutionRequest.principalId`; the view's Purpose remains fixed.

**Finding:** Principal and Purpose remain separate, additive dimensions.

## 4. Challenge: does identity substitution survive?

No `SYSTEM_PRINCIPAL_ID` or `system.knowledge-memory` literal remains in `DefaultKnowledgeCandidateEvaluator`. There is no fallback overload. Missing Principal is unrepresentable at the Kotlin contract. The dormant revision evaluator remains the sole production source occurrence and is unmodified/uncomposed.

**Finding:** live candidate identity substitution is fully removed; dormant future issue remains bounded.

## 5. Challenge: spoofing, mutation or reuse

The evaluator receives immutable `PrincipalId`. It cannot register, activate or transform it and holds no Identity Service. Submission passes the same value that its engine gate evaluated. Behavioral tests submit two different values sequentially and observe each exact value at both direct evidence and relationship retrieval, disproving cached/global reuse.

An unregistered value reaches the unchanged real engine and denies. No internal mechanism repairs it or substitutes another identity.

**Finding:** no new spoofing, mutation or fallback surface.

## 6. Challenge: hidden or ambient context

Diff and structural inspection find no global, object singleton, thread/coroutine local, metadata, reflection, stack, caller-class or class-name mechanism. The carrier is an ordinary strongly typed parameter across two adjacent calls.

**Finding:** explicit propagation only.

## 7. Challenge: broader authority or alternate path

No Principal is created or activated. No engine, Identity Service, policy, rule, Purpose registry, decorator or Memory Core path changes. Candidate retrieval continues through the one purpose-bound view, parent decorator, engine and policy. Therefore the prerequisite adds no authority of its own; the observed approval arises only from the separately preserved Unit 4 rules.

**Finding:** single authority and fail-closed identity validation preserved.

## 8. Challenge: Evidence Intelligence boundary

Evidence Intelligence production code is unchanged. Input resolution continues with its real request Principal and distinct `evidence-intelligence.input-resolution` Purpose, for which no rule approves. Its real-record composition tests pass.

Evidence Intelligence acceptance may submit a candidate using its real Principal; the later candidate-evaluation act lawfully uses that same accountable Principal under the separately bound candidate Purpose. This does not approve Evidence Intelligence input resolution.

**Finding:** Evidence Intelligence remains correctly differentiated and denied.

## 9. Challenge: dormant revision widening

`DefaultKnowledgeRevisionEvaluator` is byte-for-byte unchanged, remains uncomposed, and retains its known fixed identity only as a disclosed future governance blocker. The prerequisite adds no overload, adapter or registration that could make it live.

**Finding:** excluded dormant consumer not widened.

## 10. Challenge: accidental Unit 4 acceptance

Three Unit 4 file fingerprints remain exact. The fourth test file received only the signature adaptation forced by the accepted public contract and the prerequisite's unregistered-identity proof. `ParkerRuntime` rule content is unchanged. This review draws no conclusion on Unit 4's complete matrix or formal acceptance.

The full conversational path now succeeds because two independently visible changes coexist: preserved Unit 4 policy authority and the corrected Principal carrier. The obsolete failure assertion remains intentionally untouched. Treating that success as Unit 5 acceptance would be improper; this review does not do so.

**Finding:** prerequisite acceptance is separable from paused Unit 4 and prohibited Unit 5.

## 11. Verification classification

Direct prerequisite/adversarial set: 103 tests, 0 failures.

Full suite: 2,014 tests, two failures and eight skipped. One is the known Windows OCR path issue. The other is the historical conversational-failure expectation now contradicted by genuine cross-unit success. Neither identifies a defect in the carrier contract or propagation. Leaving the conversational assertion unchanged is the only scope-lawful choice until Unit 5 is authorized.

**Finding:** verification supports prerequisite acceptance; two failures are accurately bounded external expectations, not hidden or repaired.

## 12. Defect determination

No constitutional defect exists.

No prerequisite non-constitutional defect exists.

No corrective action or Defect Confirmation Review is required.

## 13. Constitutional verdict

```text
ACCEPTED
```

The accountable-Principal carrier prerequisite may be formally accepted. Unit 4 remains paused until explicit approval to resume it. Unit 5 has not started.
