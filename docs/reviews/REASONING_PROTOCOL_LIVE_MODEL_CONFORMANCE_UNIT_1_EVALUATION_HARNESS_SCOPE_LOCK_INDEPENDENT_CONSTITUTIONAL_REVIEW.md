**Status:** Genuine Independent Constitutional Review of the Unit 1 Scope Lock. The proposed measuring instrument was challenged directly against production contracts and consequential-action boundaries; the Scope Lock was evidence, not authority.

# Reasoning Protocol Live-Model Conformance Unit 1 Scope Lock — Independent Constitutional Review

## 1. Independent boundary reconstruction

The current production path already separates prompt construction, inference, and exact parsing. Its live-model behaviour is unqualified, while downstream Remember and Goal effects are consequential. A lawful Unit 1 may observe through the existing classes and stop at their typed result; it may not create a second reasoning path or invoke downstream coordinators.

## 2. Constitutional challenges

| Challenge | Finding |
|---|---|
| Observes rather than alters | **PASS.** Transparent capture callbacks call the production default formatter/parser and return unchanged values. Expected-action classification happens after parsing and has no return path into reasoning. |
| Could cause Remember/Goal effects | **PASS.** ParkerRuntime, reply, planner, admission, Memory and Knowledge components are forbidden. Parsed results are recorded and discarded. |
| Bypasses real prompt/parser | **PASS.** Every conformance prompt is built by `DefaultReasoningPromptBuilder`; every extracted output is parsed by `TaggedReasoningResponseParser`. A simplified manual prompt is expressly excluded. |
| Synthetic-only guarantee | **PASS.** Fixtures and every context source are repository-defined synthetic values; production import and real-content artifacts are prohibited. Writing is preceded by synthetic validation. |
| Contaminates normal builds | **PASS.** The new source set/task is not wired to `test`, `check`, `build`, or `main`. Missing live configuration skips before client creation. |
| Artifact sensitivity | **PASS.** Only synthetic prompts may be retained; endpoint credentials/query/fragment are excluded; artifacts are dedicated and untracked. |
| Malformed output fail-closed | **PASS.** Parser rejection remains authoritative. Diagnostic subclassification cannot mint a typed result. `MEMBER:` remains malformed. |
| Timeout versus semantics | **PASS.** H and I are operational outcomes and cannot enter action accuracy as model answers. |
| Unit 2 implemented prematurely | **PASS.** Repetition/profile capability is permitted; the 30-trial campaign, statistics, and model verdicts remain Unit 2. |
| Hidden remedy | **PASS.** Prompt/configuration changes, constraints, retries, repair, model switching, qualification, and timeout-default changes are explicitly forbidden. |

## 3. Additional challenge: “minimal” prompt ambiguity

The programme Planning Review named a minimal classification prompt, but Unit 1 also requires the real production builder and forbids a copied prompt. The Scope Lock resolves this correctly: `minimal-production-context` means the smallest production-generated prompt, not a manual simplified template. Production selection guidance is an invariant control, not a second injected template. This preserves measurement integrity without silently expanding scope.

## 4. Issue classification

- Need for a new live Gradle task/source set: **BOUNDARY REVIEW REQUIRED BEFORE IMPLEMENTATION**.
- Lack of a raw-envelope public return type: **NO PRODUCTION DEFECT FOR UNIT 1**; the existing formatter/parser callbacks permit transparent test-only capture.
- Optional absence of model digest/token counts: **RECORDED EVIDENCE LIMITATION**, not authority to fabricate identity or metrics.
- Existing live-model failures: **UNIT 2 CHARACTERISATION INPUT**, not a Unit 1 acceptance result.

No constitutional defect or corrective action is identified.

## 5. Verdict

```text
ACCEPTED
```

Corrective action: **NONE**. The separate Boundary Review remains a procedural prerequisite, not a correction to this Scope Lock.
