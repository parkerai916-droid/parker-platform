**Status:** Genuine Independent Constitutional Review of Unit 1 — the implementation, Gradle topology, test evidence, and exact diff were challenged directly. The Completion Review was evidence, not authority.

# Reasoning Protocol Live-Model Conformance Unit 1 Evaluation Harness — Independent Constitutional Review

## 1. Independent scope reconstruction

Unit 1 may create only a detached, synthetic, opt-in measuring instrument through the existing production reasoning boundary. It may not change production semantics, create consequential effects, introduce a remedy, or conduct Unit 2 characterisation.

Direct diff inspection finds one tracked build-file modification and two untracked implementation files under `tests/integration`. There is no production Kotlin or existing-test modification.

## 2. Constitutional challenges

| Challenge | Independent finding |
|---|---|
| Observes rather than alters | **PASS.** The real provider owns sequencing. Capture callbacks delegate to the production Ollama formatter/parser and return exactly their values. Evidence classification occurs after the provider result/failure and cannot feed back. |
| Can create Remember/Goal effects | **PASS.** The harness terminates at the typed response and has no runtime, planner, execution, admission, Memory, Knowledge, or persistence dependency. |
| Copies/bypasses prompt builder | **PASS.** Fixtures contain owner/context data only. `DefaultReasoningPromptBuilder` generates every measured prompt; a test compares the captured prompt with its direct output. |
| Bypasses parser | **PASS.** `TaggedReasoningResponseParser` is the provider collaborator. Diagnostic raw-output classification does not manufacture a response. |
| Wrappers mutate traffic | **PASS.** Request and response tests compare captured values to the exact bytes seen by the loopback endpoint and the production default functions. There is no retry. |
| Malformed/wrong outputs fail closed | **PASS.** `MEMBER:` remains parser failure C; untagged prose E; blank/partial G; multiple tags F; and valid `NOACTION`/`REPLY` for Remember remain D. None reaches downstream state. |
| Live dependencies contaminate builds | **PASS.** The source set is detached. Dry-run graphs for `test`, `check`, and `build` contain no live task. Missing live configuration skips before client construction. |
| Artifacts leak content/credentials | **PASS within frozen synthetic boundary.** Fixtures/profiles are constants marked synthetic; production configuration is never loaded; user-info/query/fragment are rejected; endpoint values are redacted from errors; output under tracked content directories is rejected. |
| Unit 2 began prematurely | **PASS.** No live configuration existed, no live smoke ran, no 30-trial cell or matrix ran, and no statistical/model conclusion was made. |
| Hidden remedy | **PASS.** No prompt, parser, sampling, schema, retry, repair, classifier, model, or production-timeout change exists. |
| Timeout measured rather than tuned | **PASS.** Per-run timeout is explicit evaluation input passed to the provider and recorded as H; Parker's default is unchanged. |
| Production changed unnecessarily | **PASS.** No `src/**` file changed. Existing public seams were sufficient. |

## 3. Adversarial evidence review

### Multiple valid-looking tags

The current parser accepts a leading recognized prefix and may retain later tagged text as content. The harness records multiple tagged lines as F and representation-invalid for evaluation while preserving the parser's actual typed result. Because the result is discarded and diagnostic classification cannot replace it, this exposes rather than repairs current behavior.

### Optional environment residue

Optional digest or image variables alone produce `Absent`, not accidental activation. Any present mandatory key with missing mandatory peers fails redacted. This satisfies “all required configuration absent means skip” while preventing partial live execution.

### Transport evidence

Raw response capture occurs inside the production client's existing response-body callback. Metadata extraction reads the captured envelope after the production extraction function returns its unchanged result. No second request or alternate transport exists.

### Ordinary-suite failure

The sole ordinary failure is the known Windows OCR separator assertion. The live source set is absent from the ordinary graph, and Unit 1 touches no OCR file. Classification: **UNRELATED ENVIRONMENTAL/PORTABILITY ISSUE** with no Unit 1 acceptance significance.

## 4. Issue and correction determination

- Unit 1 constitutional defect: **NONE**.
- Unit 1 non-constitutional defect at review boundary: **NONE**.
- Unrelated environmental/portability issue: Windows `OcrStructuralIsolationTest.kt:338`.
- Live smoke absence: **OBSERVATION ONLY / AUTHORIZED NOT EXECUTED**, not a defect.
- Defect Confirmation Review: **NOT REQUIRED** because no defect remains at completion review.

Corrective action: **NONE**.

## 5. Verdict

```text
ACCEPTED
```

Unit 1 is constitutionally complete. This acceptance authorizes no Unit 2 campaign; further work requires explicit approval to begin Unit 2 Baseline Conformance Characterisation.
