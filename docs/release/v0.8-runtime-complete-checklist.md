# v0.8-runtime-complete — Release Checklist

Prepared as part of public GitHub release preparation. No runtime
behaviour was changed; this pass covers licensing, repository hygiene,
build tooling, versioning, and documentation only.

- [x] **Apache 2.0 licence confirmed** — `LICENSE` replaced with the full
  Apache License 2.0 text. Copyright: Copyright 2026 Steven McTague.
- [x] **Local/private files removed** — `local.properties` untracked from
  git (kept on disk for local builds); `.idea/` was already untracked.
  `.gitignore` now excludes `local.properties`, `.idea/`, `.gradle/`,
  `build/`, `out/`, and `*.iml`.
- [x] **Gradle wrapper verified** — `gradlew`, `gradlew.bat`,
  `gradle/wrapper/gradle-wrapper.jar`, and
  `gradle/wrapper/gradle-wrapper.properties` are all present.
  `gradle-wrapper.jar` was missing from the working tree and has been
  restored (extracted from the official Gradle 8.10 distribution).
  `gradlew`'s line endings were normalised to LF so the script runs on a
  fresh Unix-style clone (`gradlew.bat` is unaffected).
- [x] **Version numbers aligned** — `build.gradle.kts` now sets
  `version = "0.8.0-runtime-complete"`. README and CHANGELOG both use the
  `v0.8-runtime-complete` / `0.8.0-runtime-complete` naming.
- [x] **Clean test run completed** — Test suite independently verified
  in Android Studio: 101 passing, 0 failed (see
  `docs/reviews/PARKER_PHASE_2_RUNTIME_VERIFICATION_REPORT.md` and
  `docs/reviews/PARKER_PHASE2_COMPLETION_REPORT.md`). Claude Code did
  not run `./gradlew clean test` itself — see Known Environment
  Limitations below. None of the changes in this pass touch `src/` or
  `tests/`, so this baseline is unaffected by them; re-running the
  suite in Android Studio before tagging is still recommended.
- [x] **README updated** — Status section states the open-source
  framing, `v0.8-runtime-complete` release, runtime/identity baseline
  status, 101 passing tests, and the not-yet-implemented list (agents,
  memory, world model, planner, Android integration).
- [ ] **Tag ready to create** — repository is prepared; the tag itself
  has intentionally not been created (per instructions). Recommended tag:
  `v0.8.0-runtime-complete`.

## Known Environment Limitations

Claude Code environment could not run the Gradle test task directly.
Test validation was performed independently in Android Studio.
