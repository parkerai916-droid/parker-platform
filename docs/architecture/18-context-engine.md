# Chapter 18 - Context Engine

The Context Engine determines what the user means right now. It resolves ambiguous references using recent conversation, current task, World Model, Memory, calendar, device state and location.

Context is temporary.
