# Ordinary ingestion 11R2 — V8 production execution path convergence

## Outcome

The ordinary execution composition now has an explicit, capability-bound V8
lane while historical V5 codecs, records, and readback remain available. No
provider request, owner execution, promotion, deployment, or production-store
mutation was performed.

Starting state was host `parker`, repository `/home/steve/parker-platform`,
commit/upstream `8108cd7208fc09ceafa7b50b1a3dbd772ab947a1`, clean worktree.
All work, temporary files, compilation, tests, and Git operations occurred
directly on Ubuntu; no Windows, WSL, OneDrive, SSH, or external staging was
used.

## Inventory reconciliation

The prior governed inventory of 127 V5-hard-coded matched sites across 18
files was reconciled against the current tree. Relevant sites were classified
as follows: historical V5-only (legacy codecs, acceptance records, provider
state, derivatives, and replay/readback); current V5 execution assumptions
(ordinary composition, owner projection, and active workflow); capability
neutralization required (catalogue, composition ports, derivative envelope);
V8 path already present (truthful contract, builder, validator, binder, and
fixtures); and test/fixture-only references. Historical records are never
deserialized as V8 and no immutable record is migrated.

## Convergence implemented

- Added exact V8 capability identity/catalogue projection without marking it
  production accepted.
- Added V8 owner-authorization, acceptance, guarded-execution, provider-state,
  exchange, parser/validator, reconstruction, and derivative wiring.
- Guarded preparation and request-bound checks precede reservation; raw
  provider response is atomically persisted before parsing.
- V8 provider state binds evidence, capability/digest, profile/schema/wire,
  adapter/parser/processing, provider/model/operation, request/correlation and
  execution identity, request/body/manifest digests, and response lifecycle.
- Reconstruction uses governed request-region IDs and Parker request order.
  `provider_returned_ordinal` is preserved as forensic metadata only and never
  controls identity, ordering, validity, or canonical derivative content.
- Explicit V5 readback compatibility is retained; unsupported persisted formats
  fail closed.

## Verification

Focused V8 convergence tests passed, including capability exactness,
authorization substitution rejection, canonical builder use, raw-before-parse,
malformed-response preservation, ordinal variation, Parker-order
reconstruction, and V5/V8 derivative readback. The full suite passed with
247 suites, 3,292 tests, 0 failures, 0 errors, and 17 skipped. JUnit reports
were generated by the Ubuntu `parker` test host. `git diff --check` passed.

Provider egress was zero (OpenAI 0, Claude 0, retries 0). No unit-created
watcher or background process remained. Production image, container, restart
count, and governed store counts were unchanged: image
`sha256:fdb583d16d99a58d13983046b2ad8b936014ead6b6c22cdf0d670b895b071521`,
container `281bba01fa82ddd4a172a424688845ea180a6dfe28eb4ae2aebd9c064ecd68ca`,
restart 0, stores `4 / 2 / 1 / 21 / 19 / 6 / 5`.

Accepted V8 semantics, schema, and capability digest remain unchanged. The
implementation is ready for the separately governed exact-build promotion
and deployment gate.

Exact next step: **OI11R3 — exact-build V8 production promotion/deployment
gate**, independently verify typed acceptance evidence and this implementation,
build and verify an exact-commit image, create durable capability acceptance,
deploy without owner authorization or provider egress, verify routing/readback,
and leave Sprint 2 unconsumed.
