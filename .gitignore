local.properties
.gradle/
build/
*.apk
*.aab
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
*.env
.env
secrets.*
*.keystore
*.jks
*.log
out/
dist/
tmp/

# Docling OCR bridge (tools/docling-ocr-bridge.py) -- the first Python code
# in this repository. Standard bytecode-cache artifacts, never committed.
__pycache__/
*.pyc

# Evidence Processing (Searchable PDF), Implementation Unit 2: real-world evidence corpus
# fixtures (Uber-style receipt, WelTec/ERA document, scanned/malformed/unsupported samples).
# Never committed, per Scope Lock Section 4 and Unit 7's own operational-proof requirement --
# only the small synthetic PDF fixture checked in alongside this Unit is committed to Git.
tests/fixtures/local/
